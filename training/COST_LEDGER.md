# Cost Ledger

The lightweight running cost ledger [PDR-004](../docs/decisions/PDR-004.md)
calls for — a plain log of what was spent, when, on what. Not the full
attestation ceremony the predecessor project used (see PDR-004's "Guardrail
redesign"); just enough to keep a running record. Google's platform-level
spend cap (set in AI Studio) is the hard backstop underneath this, not a
replacement for it.

Every entry below corresponds to a real, billed Gemini API call the product
owner explicitly authorized in the moment — never inferred, never carried
over from a prior approval (see `AI_COLLABORATION.md`'s "Financial
guardrails").

| Date | Model | Purpose | Requested | Accepted | Cost | Notes |
|---|---|---|---|---|---|---|
| 2026-08-19 | `gemini-3.1-pro-preview` (`pro` alias) | First test batch, v2.0 taxonomy — 15 examples, broad cognitive/emotional starter mix (calm/organized, mild distraction, hyperfocus, rapid-branching excitement, dry/neutral observation) | 15 | 13 (2 rejected on review — see `docs/datasets/CATEGORY_REFERENCE.md`) | *Not retrieved — the MCP tool call didn't return token/cost metadata. Confirm actual spend against the AI Studio billing console.* | First real, billed call under this repository. Authorized directly by the product owner ("I am ready to test this"), confirmed batch size (15) and target states via follow-up questions before the call ran. |
| 2026-08-19 | `gemini-3.1-pro-preview` (`pro` alias) | Second batch, targeted at batch 1's 3 gaps (`interrupted_thought`, `contradictory_statement`, `voice_to_text_artifact` — 2 each, with explicit guidance to avoid batch 1's specific failure modes) plus 5 previously-uncovered cognitive/emotional states (executive dysfunction, anxiety, sensory overwhelm, burnout, emotional journaling) | 15 | 14 (1 rejected — a `voice_to_text_artifact` example with one unexplainable fragment, same failure mode as batch 1's #13) | *Not retrieved — same known gap as above.* | Authorized directly ("Let's go ahead and run another batch... several examples generated before moving on"). All 3 targeted gap categories improved; `voice_to_text_artifact` still only 1-for-2 on the "No Magic Examples" bar. |
| 2026-08-19 | `gemini-3.1-pro-preview` (`pro` alias) | Third batch, part of a 2-batch authorization ("Let's run 2 more batches") — 2× `voice_to_text_artifact` (still thin), 1 each of 13 other categories, fresher subjects (home improvement, travel, fitness, hobbies, moving, pets-beyond-food, fundraising), roughly even difficulty spread | 15 | 14 (1 rejected outright — polished encyclopedia-style prose, violated "NOT polished writing"; 1 fixed in place — an invented "2x4" lumber dimension not in the input; 3 relabeled — mislabeled `interrupted_thought`/`dangling_reference`/`contradictory_statement` that were actually `topic_switching`/`self_correction`/`self_correction`) | *Not retrieved — same known gap as above.* | Recurring pattern flagged: Gemini defaults to clean-resolution `self_correction`-shaped content when asked for `contradictory_statement` or `dangling_reference` specifically — 2nd occurrence for `contradictory_statement`. Net zero new depth this batch for `interrupted_thought`/`dangling_reference`/`contradictory_statement` despite being requested. |
| 2026-08-19 | `gemini-3.1-pro-preview` (`pro` alias) | Fourth batch, 2nd of the 2-batch authorization — weighted entirely at the 7 thin categories from `CATEGORY_REFERENCE.md`'s depth tracking (`contradictory_statement` x3, `dangling_reference` x3, `simple_list`/`interrupted_thought`/`zero_action_items`/`multi_person_note` x2, `time_ambiguous` x1), with explicit contrastive definitions (what `contradictory_statement`/`dangling_reference` are NOT) replacing batch 3's plain re-statement of the category names | 15 | **15 (0 rejected, 0 relabeled at the time — but see the correction in Notes)** | *Not retrieved — same known gap as above.* | The contrastive-definition fix worked: all 3 `contradictory_statement` and all 3 `dangling_reference` examples correctly distinct this time. One soft, non-blocking note: 2 examples put a stated *wish* ("I want to cancel the trip") in `action_items` rather than just `bullets` — worth watching for consistency, not rejecting. **Correction, 2026-08-23**: this batch was recorded as the "first perfect batch" and that claim does not hold. All 15 of its narratives were written in the third person ("The author is planning a large spring vegetable garden…") rather than the writer's own voice, across 9 different categories — the single largest concentration of the narrative-voice drift found corpus-wide, and it originated here rather than at batch 5 as first thought. No review caught it for four batches, including the first adversarial re-review, which sampled 15 examples from batches 1–4. The reason is structural and is now recorded in `REVIEW_GUIDE.md` §6b: every check in items 0–5 judges one example against its own `input`, and each of these narratives was individually faithful — the defect existed only as a property of the batch. The evidence-rule findings above stand; it is the "perfect" characterization that was wrong. All 15 rewritten to first person 2026-08-23. |
| 2026-08-19 | `gemini-3.1-pro-preview` (`pro` alias) | Fifth batch, weighted at the 8 categories then at the lowest depth (`simple_list`/`topic_interleaving`/`repeated_reminder`/`rapid_branching`/`minimal_fragment`/`long_rambling`/`voice_to_text_artifact` x2, `time_ambiguous` x1), with the adversarial review's "preserve certainty level" finding baked into the prompt itself for the first time (not just checked on review) | 15 | **15 (0 rejected, 0 fixed, 0 relabeled)** | *Not retrieved — same known gap as above.* | The preventive fix worked cleanly: every hedge in every input stayed a hedge in the output (e.g. "maybe call them" correctly produced an empty `action_items`), no invented causal links, no hedge promoted to a firm task. First batch to need zero corrections of any kind. |
| 2026-08-19 | `gemini-3.1-pro-preview` (`pro` alias) | Sixth batch, weighted at the 5 remaining categories under the depth ceiling (`topic_switching`/`zero_action_items`/`multi_person_note`/`time_ambiguous`/`interrupted_thought`/`dangling_reference`/`contradictory_statement` x2, `self_correction` x1) and explicitly skewed toward `expert`/`hard` difficulty to correct a corpus-wide imbalance (`expert` was 9/71, well under the other tiers) | 15 | **15 (0 rejected, 0 fixed, 0 relabeled)** | *Not retrieved — same known gap as above.* | Hit the requested difficulty mix exactly (6 expert, 5 hard, 2 medium, 2 easy). Certainty-preservation held up even at expert complexity — nested conditionals across multiple people (e.g. "I'll call the physical therapist if Greg doesn't"), ambiguous attribution ("did Julie say she's picking up the rings or was that me?") all correctly left unresolved. Two clean `contradictory_statement` examples with no "wait/actually" reconciling language at all, a sharper distinction from `self_correction` than any prior batch achieved. |
| 2026-08-21 | `gemini-3.1-pro-preview` (`pro` alias) | Seventh batch — first run with live F.A.R.T. telemetry checkpoints (`training/telemetry.py`). Weighted at the 7 categories still at the depth floor (`simple_list`/`topic_switching`/`repeated_reminder`/`rapid_branching`/`minimal_fragment`/`long_rambling` x2, `voice_to_text_artifact` x3), skewed hard/expert again, with `action_items`-specific certainty guidance folded into the prompt directly from the second adversarial re-review's finding | 15 | **15 (0 rejected, 2 relabeled)** | *Not retrieved — same known gap as above.* | The `action_items` certainty fix held up well (hedges like "or maybe 68 degrees," "think size 7" correctly preserved even in the terse imperative field). But the *other* lesson from the second adversarial review — `topic_switching` vs. `topic_interleaving` — wasn't yet folded into this batch's prompt, and recurred exactly as predicted: both requested `topic_switching` examples were structurally interleaved (4-5 alternations, not one clean jump), relabeled to `topic_interleaving`. Net: `topic_switching` gained zero depth this batch despite being targeted — same "naming a category isn't enough" lesson as `contradictory_statement`/`dangling_reference` before it, now needs its own contrastive definition in a future batch. |
| 2026-08-23 | `gemini-3.1-pro-preview` (`pro` alias) | Eighth batch — the `topic_switching` contrastive-definition fix batch, plus depth on the four remaining 6-count categories (`topic_switching` x4, `zero_action_items` x3, `multi_person_note` x3, `time_ambiguous` x3, `interrupted_thought` x2). Prompt carried three changes over batch 7: (a) an explicit contrastive block defining `topic_switching` as a strict A-A-A-B-B-B structure with exactly one transition and zero returns, plus a per-example self-check instruction; (b) exact category counts stated as counts, with substitution forbidden; (c) a difficulty cap of medium/hard on the `topic_switching` examples specifically, with the batch's expert quota routed to the other four categories | 15 | **15 (0 rejected, 0 relabeled, 3 narrative fixes)** | *Not retrieved — same known gap as above.* | The contrastive fix worked completely: all 4 `topic_switching` examples are structurally A→B with exactly one transition and zero returns to subject A — the category finally gained real depth (5 → 9) after two consecutive batches of gaining none. Difficulty came back exactly on target (easy 1, medium 4, hard 6, expert 4). A likely root cause was identified beyond weak category definition: `TAXONOMY.md` defines `expert` as "dense combination of categories... more restated/branching content," which is *structurally* alternation — so batches 6 and 7, both of which pushed hard/expert, were giving Gemini contradictory instructions for this one category, and it resolved the conflict toward difficulty both times. Capping these examples at hard removed the conflict. Three §4 invented-content defects found and fixed by deletion (never regeneration, and nothing added): #5 invented a causal explanation ("the pacing was a bit too slow" for an input that only said "too long"), #9 invented a motive ("to maintain the surprise" — the input never says the party is a surprise), #6 guessed an unstated referent ("my old dog, Buster" — the input never says dog). All three were in `narrative` only; `bullets` and `action_items` were clean in every case, which is the inverse of batch 7's pattern and worth watching. Soft notes, not rejected: #13 lists the plumber's arrival as an `action_item` though it's an event rather than the writer's own task (same family as batch 4's "stated wish in action_items" note); #4 infers "anyone in the group" from a bare "anyone." |
| 2026-08-23 | `gemini-3.1-pro-preview` (`pro` alias) | Ninth batch — 16 requested (15 plus a replacement for the `interrupted_thought` example rejected by the third adversarial re-review). First batch to assign **difficulty per category rather than as a batch-wide skew**, following batch 8's finding that a global hard/expert push can structurally contradict a category's own definition. Cleared all nine categories that were sitting at 7. Prompt rebuilt on the corrected template and additionally dropped the template's standing "vary structure" paragraph, which would have asked for topic switches and interleaving that aren't this batch's labels — the same class of self-contradiction that cost batches 6-7 | 16 | **15 (1 rejected, 0 relabeled, 6 fixed)** | *Not retrieved — same known gap as above.* | Mechanical compliance was perfect: 16/16 on every requested category **and** difficulty tier, which no prior batch has achieved. Content was weaker than batch 8. Six fixes: two examples narrated the act of writing ("I noted that the dog needs half a scoop… but I also wrote…") — the voice drift in a **new surface form**, first person but still describing the note, and appearing only in `contradictory_statement` where presenting two conflicting statements creates pressure to narrate the conflict; one `self_correction` example preserved the retracted content its own category definition says to drop, while the other correctly dropped it (an internal inconsistency inside a single batch, caught by §6b's cross-example check); one silently resolved an ambiguous referent ("the old one" → the passport); three invented causality by converting the input's "and"/"anyway" into "so"/"since". One rejection: a `contradictory_statement` example built on two incompatible odometer readings, which is a factual impossibility rather than the "mood or stated intention that shifts partway through" the category is defined as — the same class of category mismatch that got an `interrupted_thought` example rejected the same day. **The main lesson is about the prompt, not the examples**: this was the most heavily constrained prompt yet (seven distinct rule blocks), and it bought perfect spec compliance at a visible cost in realism — several inputs read as engineered to demonstrate a category rather than as something a person typed (two adjacent sentences manufacturing a contradiction, five restatements of "email Dave" in one short note, bare comma-separated lists with no mess in them). A corpus of engineered notes will train a model that only works on engineered notes. Batch 10 should state fewer rules more briefly and add an explicit realism requirement. Also caught retroactively: the expanded voice-regression pattern found #77, whose *previous* fix (from the second adversarial re-review) had introduced the same meta-commentary along with an invented causal link — now corrected, with the input's ambiguous "I'll do it if Greg doesn't" kept verbatim rather than paraphrased. |
| 2026-08-23 | `gemini-3.1-pro-preview` (`pro` alias) | Tenth batch — first **exemplar-led** prompt, written to test whether realism responds better to examples than to rules. Roughly half the length of batch 9's: five real `input` values from the corpus shown as the standard for the field, the evidence rules compressed from seven blocks to one paragraph, and one load-bearing instruction — *do not write a note that demonstrates its category; write a note a person would actually type, which happens to have that property*. Batch 9's two worst inputs were quoted back as negative examples. Targeted the four categories clustered in one or two difficulty tiers (`voice_to_text_artifact` was hard 7 / expert 1 with no easy or medium at all) | 12 | **12 (0 rejected, 0 relabeled, 6 fixed)** | *Not retrieved — same known gap as above.* | **The realism experiment worked.** These are the most natural inputs in the corpus — the caulk/cupcakes and receipts/band/vet notes read like something actually typed, with the tangles incidental and uneven rather than end-to-end. 12/12 on category *and* difficulty, matching batch 9. But it was not free: the defect rate rose slightly, 7 defects in 12 examples (0.58 each) against batch 9's 7 in 16 (0.44). Halving the rules bought realism and cost a little rule compliance — a trade worth making, but worth naming as a trade. Fixes: meta-commentary returned in a **third distinct surface form** ("(I heard a strange noise while recording this note.)" — not third person, not "I noted that", but a parenthetical narrating the recording session); "he said" flattened to "I was told", which is the *exact* defect fixed in batch 9 six hours earlier, recurring because the fix lived in the corpus and not in the prompt; a lost fragment in a `voice_to_text_artifact` example that dropped the input's `right down` → `write down` self-correction entirely, leaving the example unable to teach its own category; two invented causalities; one dropped hedge. **New structural finding — scenario repetition is invisible to the duplicate checker.** The batch produced a "40 gal tank setup" note (corpus already had one) and a "the magic system is based on…" note (corpus already had one). Lexical similarity measured 0.14 and 0.15 against a 0.55 threshold, because the wording shares almost nothing — the *scenario* is what repeats. Both kept (two per scenario is inside the diversity rule) but a scenario-level check is now in `REVIEW_GUIDE.md` §6b. |
| 2026-08-25 | `gemini-3.1-pro-preview` (`pro` alias) | Eleventh batch — first run since 2026-08-23's voice sweep, spot-check fixes, and double-review calibration; also the first batch whose prompt carries this session's two new rules (unmarked ambiguity, no dropped imperatives) directly. Targeted the exact difficulty-tier gaps left after batch 10: `rapid_branching`/`multi_person_note`/`time_ambiguous`/`topic_interleaving` all had zero `easy` examples, `simple_list` had zero `hard`/`expert`, `contradictory_statement` had zero `expert`. Prompt kept batch 10's exemplar-led format (five real corpus inputs shown as the standard, one paragraph of evidence rules, the same load-bearing realism sentence) rather than reverting to batch 9's heavier style. Two `503 UNAVAILABLE` errors from the API before a third attempt succeeded — transient server-side load, not a prompt or setup problem; authorized to retry once by the product owner rather than falling back to `flash` | 12 | **12 (0 rejected, 0 relabeled, 4 fixed)** | *Not retrieved — same known gap as above.* | Mechanical compliance was exact: 12/12 on every requested category *and* difficulty tier, all six targeted gaps closed. **The telemetry `batch_starting()` checkpoint was missed** — the call went out before the reviewer remembered to fire it; owning this rather than backfilling a false "before" timestamp, and `batch_finished()` still ran at completion. Four fixes on review: two flattened a genuine dependency into unconditional `action_items` ("greg will do the grocery run if I text him a list" → two flat items instead of one that keeps the condition — the same class of fix `#125` needed the same day); one dropped a fragment from `action_items` that `bullets` correctly kept (milk/eggs); one reinstated two superseded instructions in `action_items` alongside the two still-live ones ("kick off tomorrow" and "hold off" both present next to "go ahead with the homepage dev") — the exact contradictory-imperatives shape `#122` was rejected for, caught here before it could recur rather than after. **Batch mean copy ratio ran high — 0.75 against the corpus's 0.56** — but reading it by category rather than as one number explains most of it: the four newly-covered `easy`-tier categories (`rapid_branching`, `multi_person_note`, `time_ambiguous`, `topic_interleaving`) sit at 0.71–0.83 because an easy-tier input in these categories is short and already close to spoken order by construction, with correspondingly little to reorganize — not the same defect as a `hard`/`expert` note copying itself. The two `contradictory_statement`/`expert` examples were a different case: both landed at 0.88, above the documented 0.85 line and *not* covered by the short-input exception (dense, multi-position notes with real structure to recover), so both narratives were rewritten to actually reorganize rather than track the input's clause order — 0.88/0.88 → 0.61/0.53. Worth tracking whether `easy`-tier requests in reorganization-heavy categories keep running structurally high; one batch isn't enough to call it a pattern yet. |
| 2026-08-25 | `gemini-3.1-pro-preview` (`pro` alias) | Twelfth batch, same day as batch 11 — floor-raising rather than gap-closing: targeted the 7 categories still sitting at 8–9 examples after batch 11 (`minimal_fragment`, `topic_switching`, `dangling_reference`, `repeated_reminder`, `zero_action_items`, `self_correction`, `interrupted_thought`), 2 each at whichever difficulty tier was thinnest within that category, rather than chasing one new ceiling. `topic_switching` capped at `hard` (not `expert`) deliberately, per batch 8's finding that `expert`'s "dense combination of categories" definition structurally conflicts with `topic_switching`'s "one clean transition, zero returns." Same exemplar-led prompt format as batches 10–11, five fresh corpus inputs. One `503 UNAVAILABLE` before a second attempt succeeded | 14 | **14 (0 rejected, 2 relabeled, 1 fixed)** | *Not retrieved — same known gap as above.* | **`minimal_fragment` gained zero depth despite being targeted twice** — the same category-definition conflict as batch 8's `topic_switching`/`expert` finding, this time self-inflicted: the prompt asked for `minimal_fragment` to reach `hard`/`expert` "through how much ambiguity... packs into very few words," but what came back were dense conditional decision trees (`"the blue one or maybe Thursday... otherwise just cancel it and use sarah's"`) — real structure, which directly contradicts the taxonomy's own "little structure" definition for this category. Both relabeled to `dangling_reference` (`hard`, `expert`) rather than rejected, since what they actually teach — preserving multiple unresolved referents without guessing — is sound, just mislabeled; `dangling_reference` picks up unplanned depth as a result (9 → 13). `minimal_fragment` stays at 8, the only category still missing `hard`/`expert` entirely, and needs a differently-shaped prompt instruction next time, not just another attempt at this one. One fix: bullets on a `zero_action_items` example used "I noted... I realized... I concluded" — the exact meta-commentary shape this project spent 2026-08-25 removing from the rest of the corpus, this time in a newly-generated example rather than a legacy one; narrative was already clean, only bullets needed the rephrase. Both telemetry checkpoints ran this time (batch 11 had missed `batch_starting()`). |
| 2026-08-25 | `gemini-3.1-pro-preview` (`pro` alias) | Fifth periodic adversarial re-review, due after batch 12 — the first run under `PDR-006`'s 2026-08-25 amendment, Gemini reviewing first against the full 0–8 checklist rather than Claude. Not a corpus batch; this is the review call itself. 13 examples: 7 edited or relabeled during this session's own first-pass review, 6 never-touched controls. One `503 UNAVAILABLE` before a second attempt succeeded | 13 reviewed | 6 ACCEPT, 7 FIX, 0 REJECT | *Not retrieved — same known gap as above.* | Full findings in `docs/datasets/REVIEW_GUIDE.md`'s adversarial-re-review log and `docs/datasets/CATEGORY_REFERENCE.md`'s fifth-re-review section. Headline: caught a category mismatch (`#153`) and an inferred-frame violation (`#163`) Claude's own first-pass review had missed minutes earlier on the same corpus; the `#163` fix briefly regressed the corpus-wide voice fix, caught immediately by re-running the standing checks. Largest finding: the field-register regression check only ever covered subject-led third-person bullets, never passive constructions with no subject — a corpus-wide scan for the missed shape found 19 lines across 18 records, all fixed, and the check itself widened. |
| 2026-09-01 | `gemini-3.1-pro-preview` (`pro` alias) | Thirteenth batch — first batch under the new "structural ceiling on `expert`" principle in `TAXONOMY.md`. Targeted `minimal_fragment` (the last category below 11, and the only one missing `hard`/`expert`) with a differently-shaped instruction: ambiguity density in very few words, explicit "no if/then branching" constraint, following the diagnosis that two prior attempts had produced real conditional structure that got relabeled away. Also 3× `simple_list` capped at `hard` (not `expert`, per the same principle), 11 more spread across the remaining categories, avoiding the flagged car-maintenance/garden scenario wells | 18 | **18 (0 rejected, 2 relabeled, 3 fixed)** | *Not retrieved — same known gap as above.* | **The `minimal_fragment`/`hard` instruction worked on the first attempt** — all 4 examples came back genuinely thin (1 line, no branching) with ambiguity carried entirely in unstated referents ("the blue one", "the other middle one", "the last batch"), closing the category's last open gap without a relabel. Two relabels on review: `#9`-equiv (paint color note) was requested/labeled `topic_switching` but the note returns to its original subject after the oven aside — textbook `interrupted_thought` (cut off, resumed), not a permanent switch; `#12`-equiv (the 2pm/3pm meeting note) was labeled `contradictory_statement` but resolves to one consistent final stance (back to 2pm) rather than preserving an unresolved tension — the exact `contradictory_statement`-vs-`self_correction` mismatch `CATEGORY_REFERENCE.md`'s fifth re-review found in `#153` five days earlier, recurring in fresh generation rather than legacy data. Three fixes: one `simple_list` example listed a restated item ("grab measuring tape" / "grab measuring tape again but the long one") as two separate bullets/action_items instead of merging per the repeated-item convention; one `simple_list` example's bullets merged a restated errand ("buy caffeine" / "get coffee again") into one line while `action_items` kept it as two — a bullets/action_items disagreement on the same fragment, a §4 "fields must agree" violation; one `rapid_branching` example's bullets used the passive "X needs to be Y-ed" shape (`"the guest room needs to be cleared out"`, `"Jamie needs to be asked..."`) that the fifth re-review had just fixed corpus-wide five days earlier — confirms the handoff's warning not to assume one fix means the passive shape can't reappear in fresh generation; both rewritten to active voice and the full voice-regression suite re-run clean afterward (0/185 on all three checks). No near-duplicates, no scenario-well hits, no mojibake. Authorized directly by the product owner ("Git it done" session, "You have authority to go ahead and proceed with both batches in order" — treated as authorizing batch 13 only, per the standing rule that blanket multi-batch pre-authorization doesn't satisfy the per-batch requirement; batch 14 to be asked for separately). |
| 2026-09-01 | `gemini-3.1-pro-preview` (`pro` alias) | Fourteenth batch — targeted `topic_switching` and `contradictory_statement`, the two categories tied at the depth floor (11) after batch 13, both with an explicit contrastive-definition instruction rather than a plain re-request. `topic_switching` capped at easy/medium (structural conflict with `expert` confirmed at batch 8, permanent). `contradictory_statement` instructed to require the note *stay* unresolved at the end — the exact defect batch 13 found in fresh generation (a note that resolves to one stance mislabeled as this category) — with an explicit contrast against `self_correction`. Remaining 12 spread across the other categories, weighted toward `expert` where the category can structurally support it. Two `503 UNAVAILABLE` before a third attempt succeeded, same transient pattern as batches 11/12/the fifth re-review | 18 | **18 (0 rejected, 2 relabeled, 3 fixed)** | *Not retrieved — same known gap as above.* | **Both contrastive-definition instructions worked cleanly.** All 3 `topic_switching` examples came back strict A-A-B with exactly one transition and zero returns, capped at easy/medium as instructed — no relabels, unlike batches 6-7's repeated failures on this exact category. All 3 `contradictory_statement` examples genuinely stayed unresolved at the end (no closing "let's just go with X"), correctly distinct from `self_correction` — the fix held on its first retry after batch 13's fresh-generation regression of the same defect. Two relabels elsewhere: one `dangling_reference` example tagged `expert` was a short flat list with one embedded unresolved reference — no dense/branching combination present, downgraded to `hard`; one `rapid_branching` example was actually a flat dump of unrelated meeting-note facts with no idea spawning the next, which is `long_rambling`'s shape, not this category's — relabeled. Three fixes: one `multi_person_note` example had a stated third-party commitment ("David said he would pay for the drinks") sitting in `bullets` but missing from `action_items` — the ownership-rule dropped-commitment failure mode, added; one `zero_action_items` example was tagged `hard` for a note with no genuine ambiguity or near-miss task language at all — downgraded to `medium`; one `interrupted_thought` example elevated a real-time in-scene request ("honey, get the door") into two forward-looking `action_items`, when it's an already-resolved momentary aside, same class as "Dr. Patel called" — removed from `action_items`, kept in `bullets`. Full voice-regression suite, schema validation, and duplicate check all clean at 203/203 after fixes. No scenario-well repetition. Authorized directly and separately from batch 13, per the standing per-batch authorization rule. |

| 2026-09-01 | `gemini-3.1-pro-preview` (`pro` alias) | Sixth periodic adversarial re-review, due after batch 14 — Gemini reviewing first against the full 0-6b checklist, then a genuinely fresh Claude subagent (no exposure to this session's reasoning) as the independent second pass, per `PDR-006`'s amendment. Not a corpus batch; this is the review call itself. 14 examples: 10 touched (relabeled or fixed) during batches 13-14's own first-pass review, 4 never-touched controls. Two `503 UNAVAILABLE` before a third attempt succeeded, same transient pattern as this session's other two calls | 14 reviewed | Gemini: 2 ACCEPT, 11 FIX/REJECT-severity. Fresh Claude: 6 ACCEPT, 8 FIX. Reconciled: 7 confirmed fixes | *Not retrieved — same known gap as above.* | Full findings in `docs/datasets/REVIEW_GUIDE.md`'s adversarial-re-review log and `docs/datasets/CATEGORY_REFERENCE.md`'s sixth-re-review section. Headline: the two independent passes disagreed on 6 of 14 examples, and reconciling that disagreement — not either pass alone — found the real defects, including two the fresh Claude pass caught alone (a dropped self-correction fragment; a `rapid_branching`/`expert` miscalibration matching this session's own new `TAXONOMY.md` principle, independently re-derived). Also caught Gemini extending a settled precedent past its documented scope twice (the `self_correction` verbatim-preservation exception; the sanctioned gerund-bullet register), both correctly held by the fresh pass. Authorized directly and separately from batches 13/14, per the standing per-batch authorization rule. |

| 2026-09-01 | `gemini-3.1-pro-preview` (`pro` alias) | Fifteenth batch — targeted `rapid_branching` and `minimal_fragment`, tied at the depth floor (12) after batch 14. `minimal_fragment` instructed to stay off `expert` and reach `hard` only through ambiguity density, never branching (repeating batch 13's successful instruction). `rapid_branching` instructed that `expert` requires a genuine second-category combination, not branching alone — the exact principle the sixth re-review's fresh-Claude pass had just independently re-derived hours earlier. Remaining 10 spread across other categories, weighted toward `expert` where structurally supported. Avoided car-maintenance, garden, dentist, and budget-spreadsheet scenarios (now well-represented) on top of the existing exclusions | 18 | **18 (0 rejected, 2 relabeled, 1 fixed)** | *Not retrieved — same known gap as above.* | **The `rapid_branching`/`expert` instruction worked immediately** — the one `expert`-tagged example combined branching with a second person's input, a hedge, and an unresolved venue decision; the other 3 stayed correctly capped at medium/hard as single-mechanism branching chains. Two relabels: one `minimal_fragment` example ("buy the grey one for him not the big one unless it's on sale") violated its own instruction by including a conditional ("unless") — the same recurring failure mode batch 12 hit — relabeled to `dangling_reference`, whose multiple genuinely unresolved referents (which grey one, which big one) fit what it actually teaches. One example requested/labeled `contradictory_statement` (Chicago packing note) resolved cleanly on every point raised (light jackets, blue sweater) with nothing left unresolved — the fourth instance of this exact category mismatch this project has hit — relabeled to `self_correction`. One fix: an `interrupted_thought` example (Q3 goals draft interrupted by laundry, cleanly resumed) was tagged `expert` for a single clean interruption-and-resume mechanism with no second category's structure combined — downgraded to `hard`, applying the same principle within hours of the sixth re-review confirming it. Full voice-regression suite, schema validation, and duplicate check clean at 221/221 after fixes. No scenario-well repetition. Authorized as one of three batches/reviews pre-authorized together in a single message; treated per the standing per-batch-authorization rule as covering this batch only — batch 16 and the next re-review to be asked for separately even though already stated. |

| 2026-09-01 | `gemini-3.1-pro-preview` (`pro` alias) | Sixteenth batch — targeted `voice_to_text_artifact`, the sole category at the depth floor (13) after batch 15, with 5 requested examples each using a different artifact type (mishearing, run-on, filler, spoken self-correction, mishearing+combination) rather than repeating one shape. Prompt also carried three explicit category-specific traps as standing guidance for the first time: `contradictory_statement` must end unresolved or self-relabel before emitting; `expert` requires genuine cross-category combination on any category, not elaboration of one mechanism; `interrupted_thought` needs an actual mid-clause break, not just an unrelated aside. Remaining 13 spread across other categories. One `503 UNAVAILABLE` before a second attempt succeeded | 18 | **18 (0 rejected, 3 relabeled, 1 fixed)** | *Not retrieved — same known gap as above.* | **All three standing traps worked as intended this time — `contradictory_statement` and `interrupted_thought` came back correctly shaped, with no repeat of prior batches' mismatches.** `voice_to_text_artifact` hit its target exactly: 5/5 with genuinely distinct artifact types, one correctly reaching `expert` by combining a mishearing with a hedge and a conditional. Three relabels: one `topic_switching` example was actually A-B-A (pottery class → passport → pottery detail, a real return to the first subject) — violates this category's own zero-returns definition from batch 8; relabeled to `topic_interleaving`. Two `expert`-tagged examples (`self_correction`: a single paint-color pivot; `topic_interleaving`: two flat threads with no combined second mechanism) were each a single mechanism, however elaborate — downgraded to `hard`, the same principle from the sixth re-review and batch 15's fix, now applied within the same batch that stated it as an explicit instruction rather than after the fact. One fix: a `voice_to_text_artifact` bullet ("Asking Sarah if the sitting times need to be adjusted") tripped the passive-voice regression check via an embedded subordinate clause — not the blatant subjectless-passive shape, but rephrased anyway per the standing practice of not arguing case-by-case with a check that exists to catch drift. Full voice-regression suite, schema validation, and duplicate check clean at 239/239. No scenario-well repetition. Authorized as one of three items given together in a single message; treated per the standing per-batch-authorization rule as covering this batch only. |

| 2026-09-01 | `gemini-3.1-pro-preview` (`pro` alias) | Seventh periodic adversarial re-review, due after batch 16 — Gemini first against the full checklist, then a genuinely fresh Claude subagent (no exposure to this session's reasoning) as the independent second pass, per `PDR-006`'s amendment. Not a corpus batch; this is the review call itself. 13 examples: 7 touched (relabeled or fixed) during batches 15-16's own first-pass review, 6 never-touched controls. Three consecutive `503 UNAVAILABLE`/`fetch failed` errors before a fourth attempt succeeded — the most retries any call needed this session | 13 reviewed | Gemini: 2 ACCEPT, 7 FIX, plus systemic over-flagging on invented causality (7 of 13). Fresh Claude: 9 ACCEPT, 4 FIX. Reconciled: 3 confirmed fixes | *Not retrieved — same known gap as above.* | Full findings in `docs/datasets/REVIEW_GUIDE.md`'s adversarial-re-review log and `docs/datasets/CATEGORY_REFERENCE.md`'s seventh-re-review section. Headline: Gemini flagged ordinary causal connectives as "invented causality" in over half the sample, even where the input's own text already stated that exact reasoning — the fresh Claude pass, given the same checklist with the rule's actual scope stated explicitly, independently confirmed the same accept/fix split, catching the overreach before it could compound into the corpus. The 3 real fixes found regardless: a `self_correction`/`expert` miscalibration made directly comparable by a same-category, same-complexity record sitting in the very same 13-example sample already correctly capped at `hard`; a literal mid-clause cutoff smoothed away in an `interrupted_thought` example, missed because the causal story still reads as recovered even without the literal broken text; and a "For work" inferred-frame violation matching this project's own worked example of the rule almost verbatim. Authorized directly and separately from batches 15/16, per the standing per-batch authorization rule. |

| 2026-09-01 | `gemini-3.1-pro-preview` (`pro` alias) | Seventeenth batch — targeted `topic_switching`, the sole category at the depth floor (14) after batch 16, and `easy` difficulty, the thinnest tier corpus-wide (42 vs. 47-81 elsewhere) after several batches weighted toward `expert`. Prompt carried the accumulated category-specific traps as standing instructions again (topic_switching zero-returns/no-expert; contradictory_statement must end unresolved; interrupted_thought needs a real cutoff preserved verbatim; expert requires named cross-category combination) | 18 | **18 (0 accepted-with-issues beyond one systemic fix, 0 rejected, 0 relabeled)** | *Not retrieved — same known gap as above.* | **All four standing traps held with zero relabels needed this time** — first batch this session with a completely clean category/difficulty read on first-pass review. All 5 `topic_switching` examples came back strict single-transition/zero-return, correctly capped at easy/medium/hard with 2 genuinely `easy`. The `interrupted_thought` example's literal dash cutoff ("I can't do the --") was preserved verbatim in narrative, bullets, and action_items — the exact convention the sixth and seventh re-reviews found missing elsewhere. **The real finding was structural, not content-level, and it was corpus-wide, not batch-specific**: reading the batch's `action_items` field as a column (per `REVIEW_GUIDE.md` §6b) found nearly every entry in this batch started lowercase ("vacuum the stairs", "text sarah about...") against the corpus's established capitalized convention — a batch-tracking drift, fixed by capitalizing all of this batch's `action_items` before acceptance. Checking whether the same shape existed pre-existing in the corpus (it does, for exactly this kind of drift) found **7 pre-existing records (lines 143, 145, 146, 147, 149, 150, 151) with the same lowercase-action-item pattern**, apparently dating to early batches and never caught by any prior review pass since no check for it existed until this session built one ad hoc — including `#151`, the "knock box" record the fifth re-review fixed for passive voice, whose fix replaced the passive phrasing but never re-capitalized it. All 7 fixed corpus-wide. One entry was deliberately excluded from the fix: `#167`'s bullet `"wait what's that alert on the monitor"` is a verbatim-preserved interruption (per the absence-representation convention) matching the input's own lowercase, run-on style — capitalizing it would have violated the convention it correctly satisfies, confirmed by checking the record's `input` directly before touching it. Full voice-regression suite, schema validation, and duplicate check clean at 257/257. No scenario-well repetition. Authorized directly for this batch alone, per the standing per-batch authorization rule. |

| 2026-09-01 | `gemini-3.1-pro-preview` (`pro` alias) | Eighteenth batch — targeted all 8 categories tied at the depth floor (16) after batch 17 with exactly 2 examples each (16 total), plus 2 more, and weighted the whole batch toward easy/medium difficulty since the corpus was still thinner on `easy` than hard/expert. Prompt carried the capitalization instruction explicitly for the first time, after batch 17's discovery that `action_items` had drifted lowercase both in that batch and, previously undetected, in 7 legacy records | 18 | **18 (0 rejected, 0 relabeled, 1 systemic fix across 3 records)** | *Not retrieved — same known gap as above.* | **Exact category-count compliance (2 each of the 8 targeted categories, both extras placed sensibly) and correct capitalization throughout** — the explicit instruction from batch 17's finding held cleanly this time, with one correct, deliberate exception (a verbatim-preserved interruption kept in its original lowercase, matching the absence-representation convention). **The one real finding was a first-person-voice gap in bare, verb-less fragments** — two `minimal_fragment` examples ("cat food chicken flavor", "jennifer's old scarf") and one `zero_action_items` example (a third-person creative-writing critique with no first-person referent at all) produced narratives with zero first-person pronoun, caught by the standing regression check. Unlike prior voice-drift instances, this wasn't describing-the-note meta-commentary — it's the genuine edge case the "essentially always contains a first-person pronoun" phrasing already anticipated: an input with no verb or personal referent at all has nothing for a faithful recovery to attach a pronoun to without inventing one. Fixed with minimal, non-inventive framing ("I need cat food in chicken flavor"; "Jennifer's old scarf is on my mind"; "I think the protagonist's motivation feels a bit weak...") that adds only the implicit personal frame every such note carries, not a specific action the input never stated. Full voice-regression suite, schema validation, and duplicate check clean at 275/275 after fixes. Noted, not actioned: "streaming subscription cancellation" and "quarterly report due Friday" scenarios are now each at 2 instances across this session's batches — within tolerance, but at the line for avoidance in future batches. Authorized directly for this batch alone, per the standing per-batch authorization rule. |

| 2026-09-01 | `gemini-3.1-pro-preview` (`pro` alias) | Eighth periodic adversarial re-review, due after batch 18 — Gemini first against the full checklist, then a genuinely fresh Claude subagent (no exposure to this session's reasoning) as the independent second pass, per `PDR-006`'s amendment. Not a corpus batch; this is the review call itself. 13 examples: 7 touched (relabeled/fixed) during batches 17-18's own first-pass review, 6 never-touched controls | 13 reviewed | Gemini: 9 ACCEPT, 2 FIX, 2 REJECT-severity (all 4 declined on reconciliation). Fresh Claude: 11 ACCEPT, 2 FIX (both confirmed). Reconciled: 2 confirmed fixes from the sample, plus 10 corpus-wide fixes from a self-directed catch made before the sample was even sent | *Not retrieved — same known gap as above.* | Full findings in `docs/datasets/REVIEW_GUIDE.md`'s adversarial-re-review log and `docs/datasets/CATEGORY_REFERENCE.md`'s eighth-re-review section. Headline: a pre-review catch found the standing passive-voice check only ever matched regular `-ed` participles, missing the identical shape with irregular verbs ("must be told/given/drawn/sent...") — 10 corpus-wide instances fixed, check widened. The formal sample then produced the widest Gemini/fresh-Claude divergence yet, and it was entirely Gemini overreach (4 of 13 flagged, all declined) — including one claim that directly contradicted this project's own documented `voice_to_text_artifact` noise-exception, traced to an incomplete checklist this session's own prompt sent to Gemini. Two real findings both reviewers agreed on: a `topic_switching` example was actually three unrelated items relabeled to `simple_list`; a `long_rambling`/`expert` example was a single mechanism downgraded to `hard`. A separate `voice_to_text_artifact`/`expert` tag was specifically re-scrutinized on request and confirmed as correctly combining two genuinely distinct mechanisms. Authorized directly and separately from batches 17/18, per the standing per-batch authorization rule. |

| 2026-09-01 | `gemini-3.1-pro-preview` (`pro` alias) | Nineteenth batch — first batch run at increased size (25, up from the 15-20 this project had used since batch 1), tested deliberately after several consecutive low-defect batches rather than assumed safe by default. Targeted `rapid_branching` and `long_rambling`, tied at the depth floor (17), each with an explicit instruction that the category's own native density (many branches, many small points) does not by itself earn `expert` — a second, named, distinct structural element is required | 25 | **25 (0 rejected, 0 relabeled, 0 fixes)** | *Not retrieved — same known gap as above.* | **The size increase produced no visible quality drop — first batch this session with zero relabels and zero fixes of any kind.** All 5 `rapid_branching` and all 5 `long_rambling` examples came back correctly capped at medium/hard except one `expert` each, both with a genuinely named second mechanism (rapid_branching combined with a hedged third-party commitment and an unattributed task; long_rambling combined with a dependent scheduling decision) rather than density alone. Capitalization, the ownership-hedge convention, and the widened passive-voice check all held throughout with no manual correction needed. Full voice-regression suite, schema validation, and duplicate check clean at 300/300 on first pass. No scenario-well repetition (11 additional exclusions from prior batches' well list observed). One judgment call worth recording: a `minimal_fragment` example ("laundry detergent") was given a filled action item where an equivalently bare fragment in batch 18 ("cat food chicken flavor") was left empty — read as a genuine difference in ambiguity (a single unqualified grocery noun has essentially one plausible reading; a flavor-qualified one plausibly reads as either a shopping note or a description of what's already being fed) rather than an inconsistency to correct. Authorized directly for this batch alone, per the standing per-batch authorization rule; the size increase itself was proposed and approved in the same conversation, not carried over from a prior approval. **Recommend keeping 25 as the new standing batch size** given this result, revisiting only if a future batch's defect rate rises. |

| 2026-09-01 | `gemini-3.1-pro-preview` (`pro` alias) | Twentieth batch — second batch at the increased size (25), targeted 6 categories tied at the depth floor (19) with exactly 3 each, plus 7 more, and deliberately weighted toward `expert` difficulty (6-8 requested) since that tier was thinnest relative to the other three — each `expert` example required naming its two combined structural elements before tagging. Excluded 15 prior scenario categories this session had already used | 25 | **25 (0 rejected, 0 relabeled, 2 fixed)** | *Not retrieved — same known gap as above.* | **The expert-weighting instruction landed close to target (7 of 25) with every single one carrying a genuinely nameable combination** — e.g. `repeated_reminder` + `dangling_reference` (a repeated wool order plus an unresolved "that other thing"), `contradictory_statement` + a second person's conflicting input (gecko-vs-dragon disagreement), `time_ambiguous` + `dangling_reference` (a vague pickup window plus an unnamed "backing fee"). Two fixes: a `multi_person_note`/`expert` example flattened "Dave is supposed to bring the folding tables" (an expectation, not a settled fact) into "Dave to bring the folding tables" in `action_items` — restored the hedge, matching the exact convention this session has repeatedly reinforced. A `simple_list` example's narrative invented "For the beach day" as a frame — the input (sunscreen, towels, cooler, sunglasses) never states "beach" anywhere, matching this project's own canonical banned-inference pattern almost exactly; removed. Full voice-regression suite, schema validation, and duplicate check clean at 325/325 after fixes. **Noted, not actioned**: a `topic_interleaving` example's Python-syntax-error-on-line-42 scenario is suspiciously close to batch 19's own Python debugging example one batch earlier — still only the second instance and the surrounding content diverges enough (cooking dinner vs. a cat knocking over a glass) to stay within tolerance, but "Python debugging"/"line 42" specifically should be excluded from future batch prompts given it's now recurred immediately. Authorized directly for this batch alone, per the standing per-batch authorization rule. |

| 2026-09-01 | `gemini-3.1-pro-preview` (`pro` alias) | Ninth periodic adversarial re-review, due after batch 20 — Gemini first against the full checklist, then a genuinely fresh Claude subagent (no exposure to this session's reasoning) as the independent second pass, per `PDR-006`'s amendment. Not a corpus batch; this is the review call itself. 14 examples: 2 touched (fixed) during batch 20's own first-pass review, 12 never-touched controls, weighted toward that batch's `expert`-tagged examples | 14 reviewed | Gemini: 5 ACCEPT, 8 FIX, 1 REJECT, plus a blanket 6-example calibration claim. Fresh Claude: 5 ACCEPT, 9 FIX, a narrower 5-example calibration claim. Reconciled: 5 confirmed difficulty downgrades, 7 confirmed content fixes | *Not retrieved — same known gap as above.* | Full findings in `docs/datasets/REVIEW_GUIDE.md`'s adversarial-re-review log and `docs/datasets/CATEGORY_REFERENCE.md`'s ninth-re-review section. Headline: the sharpest convergence yet between the two independent passes — both found the same two inferred-frame violations ("community," "cooking dinner") almost verbatim matching this project's own canonical examples, and the same hedge-dropping pattern across 4 examples. The two diverged on `self_correction`: Gemini's REJECT directly contradicted this project's own settled "drop retracted content" convention for that category, while the fresh pass, given the exemption stated explicitly, found the real, narrower defect (the narrative alone reintroduced a number bullets/action_items had already correctly dropped). On `expert` calibration, the fresh pass's per-example reasoning beat both Gemini's blanket claim and its own first instinct, downgrading 5 genuinely single-mechanism examples while explicitly defending 4 others with real nameable combinations. Authorized directly and separately from batches 19/20, per the standing per-batch authorization rule. |

## Known gap

This ledger's `Cost` column can't be filled in from the MCP tool's response
alone — `gemini-query` doesn't return usage/billing metadata. Until that's
resolved (either the MCP server exposes it, or costs are checked manually
in AI Studio after each call), treat this column as informational-only, not
a substitute for checking the actual AI Studio billing console
periodically.
