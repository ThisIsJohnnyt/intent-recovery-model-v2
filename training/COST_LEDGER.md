# Cost Ledger

The lightweight running cost ledger [PDR-004](../docs/decisions/PDR-004.md)
calls for — a plain log of what was spent, when, on what. Not the full
attestation ceremony the predecessor project used (see PDR-004's "Guardrail
redesign"); just enough to keep a running record. Google's platform-level
spend cap (set in AI Studio) is the hard backstop underneath this, not a
replacement for it.

Every entry below corresponds to a real, billed Gemini API call the product
owner explicitly authorized in the moment — never inferred, never carried
over from a prior approval (see `AI_COLLABORATION.md`'s "Financial
guardrails").

| Date | Model | Purpose | Requested | Accepted | Cost | Notes |
|---|---|---|---|---|---|---|
| 2026-08-19 | `gemini-3.1-pro-preview` (`pro` alias) | First test batch, v2.0 taxonomy — 15 examples, broad cognitive/emotional starter mix (calm/organized, mild distraction, hyperfocus, rapid-branching excitement, dry/neutral observation) | 15 | 13 (2 rejected on review — see `docs/datasets/CATEGORY_REFERENCE.md`) | *Not retrieved — the MCP tool call didn't return token/cost metadata. Confirm actual spend against the AI Studio billing console.* | First real, billed call under this repository. Authorized directly by the product owner ("I am ready to test this"), confirmed batch size (15) and target states via follow-up questions before the call ran. |
| 2026-08-19 | `gemini-3.1-pro-preview` (`pro` alias) | Second batch, targeted at batch 1's 3 gaps (`interrupted_thought`, `contradictory_statement`, `voice_to_text_artifact` — 2 each, with explicit guidance to avoid batch 1's specific failure modes) plus 5 previously-uncovered cognitive/emotional states (executive dysfunction, anxiety, sensory overwhelm, burnout, emotional journaling) | 15 | 14 (1 rejected — a `voice_to_text_artifact` example with one unexplainable fragment, same failure mode as batch 1's #13) | *Not retrieved — same known gap as above.* | Authorized directly ("Let's go ahead and run another batch... several examples generated before moving on"). All 3 targeted gap categories improved; `voice_to_text_artifact` still only 1-for-2 on the "No Magic Examples" bar. |
| 2026-08-19 | `gemini-3.1-pro-preview` (`pro` alias) | Third batch, part of a 2-batch authorization ("Let's run 2 more batches") — 2× `voice_to_text_artifact` (still thin), 1 each of 13 other categories, fresher subjects (home improvement, travel, fitness, hobbies, moving, pets-beyond-food, fundraising), roughly even difficulty spread | 15 | 14 (1 rejected outright — polished encyclopedia-style prose, violated "NOT polished writing"; 1 fixed in place — an invented "2x4" lumber dimension not in the input; 3 relabeled — mislabeled `interrupted_thought`/`dangling_reference`/`contradictory_statement` that were actually `topic_switching`/`self_correction`/`self_correction`) | *Not retrieved — same known gap as above.* | Recurring pattern flagged: Gemini defaults to clean-resolution `self_correction`-shaped content when asked for `contradictory_statement` or `dangling_reference` specifically — 2nd occurrence for `contradictory_statement`. Net zero new depth this batch for `interrupted_thought`/`dangling_reference`/`contradictory_statement` despite being requested. |
| 2026-08-19 | `gemini-3.1-pro-preview` (`pro` alias) | Fourth batch, 2nd of the 2-batch authorization — weighted entirely at the 7 thin categories from `CATEGORY_REFERENCE.md`'s depth tracking (`contradictory_statement` x3, `dangling_reference` x3, `simple_list`/`interrupted_thought`/`zero_action_items`/`multi_person_note` x2, `time_ambiguous` x1), with explicit contrastive definitions (what `contradictory_statement`/`dangling_reference` are NOT) replacing batch 3's plain re-statement of the category names | 15 | **15 (0 rejected, 0 relabeled at the time — but see the correction in Notes)** | *Not retrieved — same known gap as above.* | The contrastive-definition fix worked: all 3 `contradictory_statement` and all 3 `dangling_reference` examples correctly distinct this time. One soft, non-blocking note: 2 examples put a stated *wish* ("I want to cancel the trip") in `action_items` rather than just `bullets` — worth watching for consistency, not rejecting. **Correction, 2026-08-23**: this batch was recorded as the "first perfect batch" and that claim does not hold. All 15 of its narratives were written in the third person ("The author is planning a large spring vegetable garden…") rather than the writer's own voice, across 9 different categories — the single largest concentration of the narrative-voice drift found corpus-wide, and it originated here rather than at batch 5 as first thought. No review caught it for four batches, including the first adversarial re-review, which sampled 15 examples from batches 1–4. The reason is structural and is now recorded in `REVIEW_GUIDE.md` §6b: every check in items 0–5 judges one example against its own `input`, and each of these narratives was individually faithful — the defect existed only as a property of the batch. The evidence-rule findings above stand; it is the "perfect" characterization that was wrong. All 15 rewritten to first person 2026-08-23. |
| 2026-08-19 | `gemini-3.1-pro-preview` (`pro` alias) | Fifth batch, weighted at the 8 categories then at the lowest depth (`simple_list`/`topic_interleaving`/`repeated_reminder`/`rapid_branching`/`minimal_fragment`/`long_rambling`/`voice_to_text_artifact` x2, `time_ambiguous` x1), with the adversarial review's "preserve certainty level" finding baked into the prompt itself for the first time (not just checked on review) | 15 | **15 (0 rejected, 0 fixed, 0 relabeled)** | *Not retrieved — same known gap as above.* | The preventive fix worked cleanly: every hedge in every input stayed a hedge in the output (e.g. "maybe call them" correctly produced an empty `action_items`), no invented causal links, no hedge promoted to a firm task. First batch to need zero corrections of any kind. |
| 2026-08-19 | `gemini-3.1-pro-preview` (`pro` alias) | Sixth batch, weighted at the 5 remaining categories under the depth ceiling (`topic_switching`/`zero_action_items`/`multi_person_note`/`time_ambiguous`/`interrupted_thought`/`dangling_reference`/`contradictory_statement` x2, `self_correction` x1) and explicitly skewed toward `expert`/`hard` difficulty to correct a corpus-wide imbalance (`expert` was 9/71, well under the other tiers) | 15 | **15 (0 rejected, 0 fixed, 0 relabeled)** | *Not retrieved — same known gap as above.* | Hit the requested difficulty mix exactly (6 expert, 5 hard, 2 medium, 2 easy). Certainty-preservation held up even at expert complexity — nested conditionals across multiple people (e.g. "I'll call the physical therapist if Greg doesn't"), ambiguous attribution ("did Julie say she's picking up the rings or was that me?") all correctly left unresolved. Two clean `contradictory_statement` examples with no "wait/actually" reconciling language at all, a sharper distinction from `self_correction` than any prior batch achieved. |
| 2026-08-21 | `gemini-3.1-pro-preview` (`pro` alias) | Seventh batch — first run with live F.A.R.T. telemetry checkpoints (`training/telemetry.py`). Weighted at the 7 categories still at the depth floor (`simple_list`/`topic_switching`/`repeated_reminder`/`rapid_branching`/`minimal_fragment`/`long_rambling` x2, `voice_to_text_artifact` x3), skewed hard/expert again, with `action_items`-specific certainty guidance folded into the prompt directly from the second adversarial re-review's finding | 15 | **15 (0 rejected, 2 relabeled)** | *Not retrieved — same known gap as above.* | The `action_items` certainty fix held up well (hedges like "or maybe 68 degrees," "think size 7" correctly preserved even in the terse imperative field). But the *other* lesson from the second adversarial review — `topic_switching` vs. `topic_interleaving` — wasn't yet folded into this batch's prompt, and recurred exactly as predicted: both requested `topic_switching` examples were structurally interleaved (4-5 alternations, not one clean jump), relabeled to `topic_interleaving`. Net: `topic_switching` gained zero depth this batch despite being targeted — same "naming a category isn't enough" lesson as `contradictory_statement`/`dangling_reference` before it, now needs its own contrastive definition in a future batch. |
| 2026-08-23 | `gemini-3.1-pro-preview` (`pro` alias) | Eighth batch — the `topic_switching` contrastive-definition fix batch, plus depth on the four remaining 6-count categories (`topic_switching` x4, `zero_action_items` x3, `multi_person_note` x3, `time_ambiguous` x3, `interrupted_thought` x2). Prompt carried three changes over batch 7: (a) an explicit contrastive block defining `topic_switching` as a strict A-A-A-B-B-B structure with exactly one transition and zero returns, plus a per-example self-check instruction; (b) exact category counts stated as counts, with substitution forbidden; (c) a difficulty cap of medium/hard on the `topic_switching` examples specifically, with the batch's expert quota routed to the other four categories | 15 | **15 (0 rejected, 0 relabeled, 3 narrative fixes)** | *Not retrieved — same known gap as above.* | The contrastive fix worked completely: all 4 `topic_switching` examples are structurally A→B with exactly one transition and zero returns to subject A — the category finally gained real depth (5 → 9) after two consecutive batches of gaining none. Difficulty came back exactly on target (easy 1, medium 4, hard 6, expert 4). A likely root cause was identified beyond weak category definition: `TAXONOMY.md` defines `expert` as "dense combination of categories... more restated/branching content," which is *structurally* alternation — so batches 6 and 7, both of which pushed hard/expert, were giving Gemini contradictory instructions for this one category, and it resolved the conflict toward difficulty both times. Capping these examples at hard removed the conflict. Three §4 invented-content defects found and fixed by deletion (never regeneration, and nothing added): #5 invented a causal explanation ("the pacing was a bit too slow" for an input that only said "too long"), #9 invented a motive ("to maintain the surprise" — the input never says the party is a surprise), #6 guessed an unstated referent ("my old dog, Buster" — the input never says dog). All three were in `narrative` only; `bullets` and `action_items` were clean in every case, which is the inverse of batch 7's pattern and worth watching. Soft notes, not rejected: #13 lists the plumber's arrival as an `action_item` though it's an event rather than the writer's own task (same family as batch 4's "stated wish in action_items" note); #4 infers "anyone in the group" from a bare "anyone." |
| 2026-08-23 | `gemini-3.1-pro-preview` (`pro` alias) | Ninth batch — 16 requested (15 plus a replacement for the `interrupted_thought` example rejected by the third adversarial re-review). First batch to assign **difficulty per category rather than as a batch-wide skew**, following batch 8's finding that a global hard/expert push can structurally contradict a category's own definition. Cleared all nine categories that were sitting at 7. Prompt rebuilt on the corrected template and additionally dropped the template's standing "vary structure" paragraph, which would have asked for topic switches and interleaving that aren't this batch's labels — the same class of self-contradiction that cost batches 6-7 | 16 | **15 (1 rejected, 0 relabeled, 6 fixed)** | *Not retrieved — same known gap as above.* | Mechanical compliance was perfect: 16/16 on every requested category **and** difficulty tier, which no prior batch has achieved. Content was weaker than batch 8. Six fixes: two examples narrated the act of writing ("I noted that the dog needs half a scoop… but I also wrote…") — the voice drift in a **new surface form**, first person but still describing the note, and appearing only in `contradictory_statement` where presenting two conflicting statements creates pressure to narrate the conflict; one `self_correction` example preserved the retracted content its own category definition says to drop, while the other correctly dropped it (an internal inconsistency inside a single batch, caught by §6b's cross-example check); one silently resolved an ambiguous referent ("the old one" → the passport); three invented causality by converting the input's "and"/"anyway" into "so"/"since". One rejection: a `contradictory_statement` example built on two incompatible odometer readings, which is a factual impossibility rather than the "mood or stated intention that shifts partway through" the category is defined as — the same class of category mismatch that got an `interrupted_thought` example rejected the same day. **The main lesson is about the prompt, not the examples**: this was the most heavily constrained prompt yet (seven distinct rule blocks), and it bought perfect spec compliance at a visible cost in realism — several inputs read as engineered to demonstrate a category rather than as something a person typed (two adjacent sentences manufacturing a contradiction, five restatements of "email Dave" in one short note, bare comma-separated lists with no mess in them). A corpus of engineered notes will train a model that only works on engineered notes. Batch 10 should state fewer rules more briefly and add an explicit realism requirement. Also caught retroactively: the expanded voice-regression pattern found #77, whose *previous* fix (from the second adversarial re-review) had introduced the same meta-commentary along with an invented causal link — now corrected, with the input's ambiguous "I'll do it if Greg doesn't" kept verbatim rather than paraphrased. |

## Known gap

This ledger's `Cost` column can't be filled in from the MCP tool's response
alone — `gemini-query` doesn't return usage/billing metadata. Until that's
resolved (either the MCP server exposes it, or costs are checked manually
in AI Studio after each call), treat this column as informational-only, not
a substitute for checking the actual AI Studio billing console
periodically.
